var quotes = function(container)  {
  this.quotes = document.createElement('div');
  this.quotesElem.classList.add('weather');
  this.quotesElem.id = 'weather';

  container.appendChild(this.quotesElem);
};
